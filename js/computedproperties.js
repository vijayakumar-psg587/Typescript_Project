var osversion = 'os_';
var supporetedObj = (_a = {},
    _a[osversion + 'Windows'] = isSupported('Windows'),
    _a[osversion + 'IOS'] = isSupported('iOS'),
    _a);
function isSupported(test) {
    return true;
}
var _a;
